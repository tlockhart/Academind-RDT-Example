export const getUsersReducer = (state, action) => {
  console.log("GETUDERS state:", state);
  // switch (action.type) {
  //   case "GET_USERS":
    //   return {
    //     ...state,
    //   };
    // default:
      // return default state
      return state;
  // }
};
